import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App initializes', (WidgetTester tester) async {
    // Basic smoke test - verifies imports compile correctly.
    expect(true, isTrue);
  });
}
